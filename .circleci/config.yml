version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:11.0.0-jdk
    working_directory: ~/scheduler
    steps:
      - run: ./gradlew build

workflows:
  version: 2
  build:
    jobs:
      - build